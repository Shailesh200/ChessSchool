#!/usr/bin/env node
/**
 * Fairytale pieces are hand-authored SVGs in public/pieces/fairytale/.
 * Do not run this script — it would overwrite the curated set.
 * Edit the .svg files directly instead.
 */
console.error(
  "Fairytale SVGs are hand-maintained in public/pieces/fairytale/. Edit those files directly.",
);
process.exit(1);
