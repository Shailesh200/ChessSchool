# Legacy Lichess CSV import — use `pnpm db:import-puzzle-school` instead.
# `import-lichess.mjs` remains the implementation; this folder documents the deprecation path.
# Puzzle School JSONL bank will replace direct Lichess bucketing in M-063 Phase 3.
